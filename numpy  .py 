import numpy as np
marks = np.array([78, 85, 62, 90, 88, 76, 95, 67])

print("Marks:", marks)
print("Average Marks:", np.mean(marks))
print("Highest Marks:", np.max(marks))
print("Lowest Marks:", np.min(marks))

grades = np.where(marks >= 75, "Pass", "Fail")
print("Result:", grades)