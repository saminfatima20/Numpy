import numpy as np

sales = np.array([
    [200, 220, 250, 300, 280],
    [150, 180, 170, 160, 190],
    [400, 420, 410, 430, 450]
])

print("Total sales per product:", np.sum(sales, axis=1))
print("Total sales per day:", np.sum(sales, axis=0))
print("Average daily sales:", np.mean(sales))
